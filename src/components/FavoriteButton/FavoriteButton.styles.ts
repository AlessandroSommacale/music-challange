import styled from 'styled-components';

export const FavoriteButtonContainer = styled.button`
  position: absolute;
  top: 8px;
  right: 8px;
  z-index: 1;
  background: none;
  border: none;
  width: 24px;
  height: 24px;

  svg {
    cursor: pointer;
    width: 24px;
    height: 24px;
  }
`;
